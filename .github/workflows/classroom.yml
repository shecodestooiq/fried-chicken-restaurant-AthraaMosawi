name: Autograding Tests
'on':
- push
- workflow_dispatch
- repository_dispatch
permissions:
  checks: write
  actions: read
  contents: read
jobs:
  run-autograding-tests:
    runs-on: ubuntu-latest
    if: github.actor != 'github-classroom[bot]'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: index.test.js
      id: index-test-js
      uses: education/autograding-command-grader@v1
      with:
        test-name: index.test.js
        setup-command: npm install
        command: npm test a
        timeout: 60
        max-score: 100
    - name: Autograding Reporter
      uses: education/autograding-grading-reporter@v1
      env:
        INDEX-TEST-JS_RESULTS: "${{steps.index-test-js.outputs.result}}"
      with:
        runners: index-test-js
